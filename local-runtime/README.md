# Generated local AI runtime

`npm run runtime:build` creates the platform-specific `dashcut-transcribe` bundle in this directory. Model weights are deliberately excluded and are downloaded only after the user clicks install in DashCut.
